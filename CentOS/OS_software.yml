---
- hosts: all

  tasks:
  - name: Repo epel-release
    yum:
       name: epel-release
       state: latest

  - name: install packages
    # bash-completion --- bash 补全
    # htop            --- top 改进版
    yum:
       name:
         - bash-completion
         - htop
         - net-tools
         - tar
         - vim
         - wget
       state: latest
